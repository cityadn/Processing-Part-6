String moduleTitle = "Coding bootcamp";
println(moduleTitle);
println(moduleTitle.length());

/* A class is like a normal variable type on steroids — not 
only can it be used for storing things, it also has 
its own methods for doing stuff with the data it stores. 
You can access those methods by using the 'dot operator' 
appended to the name of the variable. */
